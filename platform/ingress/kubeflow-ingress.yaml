apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kubeflow
  namespace: istio-system
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - kubeflow.erotica.icu
      secretName: erotica-icu-tls
  rules:
    - host: kubeflow.erotica.icu
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: istio-ingressgateway
                port:
                  number: 80
